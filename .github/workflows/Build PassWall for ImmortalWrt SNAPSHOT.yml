name: Build PassWall for ImmortalWrt SNAPSHOT

on:
  workflow_dispatch:
    inputs:
      target_arch:
        description: '目标架构（如 x86_64/armvirt-64/mipsel_24kc）'
        required: true
        default: 'x86_64'

env:
  PASSWALL_REPO: "https://github.com/Openwrt-Passwall/openwrt-passwall.git"
  PASSWALL_PACKAGES_REPO: "https://github.com/Openwrt-Passwall/openwrt-passwall-packages.git"
  IMMORTALWRT_REPO: "https://github.com/immortalwrt/immortalwrt.git"
  IMMORTALWRT_BRANCH: "master"

jobs:
  build-passwall:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: 清理残留目录
        run: rm -rf ./immortalwrt

      - name: 安装最小化编译环境
        run: |
          sudo apt update -y
          sudo apt install -y --no-install-recommends \
            build-essential libncurses5-dev libncursesw5-dev zlib1g-dev \
            gawk git gettext libssl-dev xsltproc wget unzip python3 curl \
            flex bison ccache automake libtool binutils-dev bc patch \
            file libelf-dev openssl gzip sed zstd
          sudo apt clean

      - name: 克隆ImmortalWrt源码（仅核心文件）
        run: |
          # 浅克隆，只拉取必要文件
          git clone --depth 1 --single-branch --branch $IMMORTALWRT_BRANCH $IMMORTALWRT_REPO immortalwrt
          cd immortalwrt
          # 只更新luci和passwall相关feeds，跳过其他
          echo "src-git luci https://github.com/immortalwrt/luci.git" > feeds.conf.default
          echo "src-git passwall $PASSWALL_REPO" >> feeds.conf.default
          echo "src-git passwall_packages $PASSWALL_PACKAGES_REPO" >> feeds.conf.default
          ./scripts/feeds update luci passwall passwall_packages
          ./scripts/feeds install -a -p luci
          ./scripts/feeds install -a -p passwall

      - name: 配置极简编译参数（仅PassWall）
        run: |
          cd immortalwrt
          TARGET_ARCH="${{ github.event.inputs.target_arch }}"
          [ -z "$TARGET_ARCH" ] && TARGET_ARCH="x86_64"
          
          # 基础架构配置
          case $TARGET_ARCH in
            x86_64)
              echo -e "CONFIG_TARGET_x86=y\nCONFIG_TARGET_x86_64=y\nCONFIG_TARGET_x86_64_Default=y" > .config
              ;;
            armvirt-64)
              echo -e "CONFIG_TARGET_armvirt=y\nCONFIG_TARGET_armvirt_64=y\nCONFIG_TARGET_armvirt_64_Default=y" > .config
              ;;
            mipsel_24kc)
              echo -e "CONFIG_TARGET_ramips=y\nCONFIG_TARGET_ramips_mt7620=y\nCONFIG_TARGET_ramips_mt7620_Default=y" > .config
              ;;
            *)
              echo "不支持的架构: $TARGET_ARCH" && exit 1
              ;;
          esac

          # 仅启用PassWall必需配置
          echo -e "CONFIG_PACKAGE_luci=y" >> .config
          echo -e "CONFIG_PACKAGE_luci-base=y" >> .config
          echo -e "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
          echo -e "CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y" >> .config
          # 禁用所有不必要的功能
          echo -e "CONFIG_ALL_KMODS=n\nCONFIG_ALL_NONSHARED=n\nCONFIG_ALL_PACKAGES=n" >> .config
          echo -e "CONFIG_PACKAGE_apk=n" >> .config  # 显式禁用apk包
          make defconfig

      - name: 修复编译工具链
        run: |
          cd immortalwrt
          mkdir -p staging_dir/host/bin
          # 映射核心工具
          ln -sf /usr/bin/openssl staging_dir/host/bin/openssl
          ln -sf /usr/bin/gzip staging_dir/host/bin/libdeflate-gzip
          ln -sf /usr/bin/sed staging_dir/host/bin/sed
          ln -sf /usr/bin/gcc staging_dir/host/bin/gcc
          ln -sf /usr/bin/zstdcat staging_dir/host/bin/zstdcat

      - name: 编译PassWall（强制跳过无关依赖）
        run: |
          cd immortalwrt
          # 直接编译PassWall，忽略依赖检查
          make package/luci-app-passwall/compile V=s -j1 IGNORE_ERRORS=1

      - name: 收集编译产物
        run: |
          mkdir -p passwall-ipk
          find immortalwrt/bin/packages -name "luci-app-passwall*" -type f -exec cp {} passwall-ipk/ \;
          echo "编译完成！产物列表："
          ls -lh passwall-ipk/ || echo "未找到产物（可能编译仍有小问题）"

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: passwall-immortalwrt-${{ github.event.inputs.target_arch }}
          path: passwall-ipk/
